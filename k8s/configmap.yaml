apiVersion: v1
kind: ConfigMap
metadata:
  name: grokforge-config
  namespace: grokforge
data:
  NODE_ENV: "production"
  PORT: "3000"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  OLLAMA_HOST: "ollama-service"
  OLLAMA_PORT: "11434"
  QUEUE_API_URL: "http://queue-service:3001"
  # Redis password (if needed, use Secret instead)
  # REDIS_PASSWORD: ""
  # OLLAMA_MODEL: "qwen3:latest"

